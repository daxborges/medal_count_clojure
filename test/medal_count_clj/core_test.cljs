(ns medal-count-clj.core-test
  (:require                                                 ;[clojure.test :refer :all]
            [cljs.test :refer-macros [deftest is testing run-tests]]
    ;[medal-count-clj.core]
            ))

(deftest a-test-b
  (testing "FIXME, I pass."
    (is (= 1 2))))

(deftest a-test
  (testing "FIXME, I fail 2."
    (is (= 2 1))))
